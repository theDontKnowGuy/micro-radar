# Dual-slot OTA layout for an 8MB flash, replacing huge_app.csv.
#
# huge_app declares a single app partition (ota_0 only), so there is nowhere to
# write a downloaded image and self-updating is impossible with it. This table
# declares ota_0 and ota_1, and the bootloader alternates between them: the
# running firmware writes the new image into the slot it is not executing from,
# flips otadata, and reboots. A download that fails or is interrupted leaves the
# running slot untouched, so a dropped Wi-Fi connection mid-update is harmless.
#
# Each app slot is 0x330000 = 3,342,336 bytes. The 360x360 build is around
# 1.3MB, so roughly 40% is in use -- there is a lot of headroom for growth.
#
# Sized for 8MB because that is the smaller of the two possible modules; the
# table is equally valid on a 16MB part, it just leaves the top 8MB unused. If
# you confirm 16MB flash (esptool.py flash_id) you can grow both app slots.
#
# App partitions must start on a 0x10000 boundary -- keep that in mind if you
# resize anything above them.
#
# Changing the partition table erases NVS, so Wi-Fi credentials and all settings
# saved through the web UI are lost the first time you flash this over serial.
# They survive every subsequent OTA, because OTA only rewrites an app slot.
#
# Moving to this table also relocates the coredump partition from 0x3F0000 to
# 0x7F0000, which under huge_app.csv was past the end of the table and still
# holds whatever was flashed there before. The core dump component reads that
# stale data as a length and complains once per boot:
#
#   E (253) esp_core_dump_flash: Incorrect size of core dump image: 1819042143
#
# Harmless -- it only means "no valid core dump here" -- but to silence it,
# erase the region once (an *erased* partition is skipped without logging):
#
#   esptool.py --chip esp32s3 --port <port> erase_region 0x7F0000 0x10000

# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x5000,
otadata,    data, ota,     0xe000,   0x2000,
app0,       app,  ota_0,   0x10000,  0x330000,
app1,       app,  ota_1,   0x340000, 0x330000,
spiffs,     data, spiffs,  0x670000, 0x180000,
coredump,   data, coredump,0x7F0000, 0x10000,
